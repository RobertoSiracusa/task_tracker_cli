[package]
name = "task_tracker_cli"
version = "0.1.0"
edition = "2024"

[dependencies]
chrono = { version = "0.4.44", features = ["serde"] }

serde = { version = "1.0", features = ["derive"] }
# Añade un formato, por ejemplo, serde_json para JSON
serde_json = "1.0"
